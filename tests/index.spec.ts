import { it } from "node:test";
import { describe } from "vitest";
import { expect } from "vitest";

describe('index.ts', () => {
    it('should run', () => {
        expect(true).toBeFalsy();
    });
});